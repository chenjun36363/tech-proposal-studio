# TechProposal Studio 项目待办

> 最后整理：2026-07-31。本文档是根目录唯一的项目待办来源；实现细节发生变化时同步更新本文件。

## 执行原则

- 正文和索引默认留在本机；AI 只接收当前任务需要的正文和用户明确附加的资料。
- 所有 AI 正文修改先生成可审核提案，用户接受后才更新编辑器状态。
- 接受 AI 提案不直接写入工作区文件，磁盘写入继续由“保存”操作触发。
- API Key 不得写入项目 localStorage 缓存。
- 每项功能完成前补齐相应测试，并运行 `pnpm test` 和 `pnpm build`。

## 近期优先顺序

当前采用“AI 能力优先”顺序；工作包编号保持稳定，实际依次推进：

1. P3 Agent 文档编辑工具：先扩展章节、选区、插入和删除能力，所有变更保持可审核、可冲突检测。
2. P4 全文优化：在安全编辑工具之上增加全文规划、逐章生成和一致性复核。
3. P1/P2 能力基础设施：随后补强会话可靠性和工作区实时同步，确保长任务和外部编辑安全。

## P7 源码结构治理

目标：在不改变产品行为、不阻塞 P1-P4 功能开发的前提下，逐步收敛前端入口、全局样式和运行时边界。每批迁移保持可独立合并，禁止将纯结构调整与功能行为修改混在同一提交中。

### 执行约束

- [x] 建立重构前基线：38 个测试文件、231 项测试通过，`pnpm build` 通过；基线文件规模为 `App.tsx` 1331 行、`index.css` 4868 行、`docxExport.ts` 828 行、Rust `lib.rs` 641 行。
- [x] 按功能完成物理目录分区：`src/` 根目录只保留应用入口，`src-tauri/src/` 根目录只保留 crate 入口；业务分别归入 `core/features/agent/services` 与 `agent/workspace/integrations/platform/knowledge`。
- [ ] 固定依赖方向为 `app -> features -> domain/shared/services`；领域纯函数不得依赖 React、Tauri 或具体存储实现。
- [ ] 一个迁移批次只处理一个明确边界；完成后运行 `pnpm test` 和 `pnpm build`。
- [ ] 新增代码不得继续堆入 `App.tsx` 或 `index.css`；共享代码必须存在至少两个真实消费者。

### P7.1 样式边界

- [ ] 将 Git、设置、技能、编辑器和应用壳样式从 `index.css` 迁入对应 feature/component 样式文件。
- [ ] `index.css` 最终只保留设计变量、全局重置、字体、通用控件和应用基础布局。
- [ ] 合并重复选择器和文件末尾的历史覆盖层，迁移期间保持现有视觉与响应式行为不变。
- [ ] 为关键桌面与窄屏视口补充截图检查，确认面板、弹窗和编辑区无重叠或溢出。

### P7.2 应用入口与编辑工作区

- [x] 恢复 `PowerShellTerminal` 到右侧 inspector 的终端页签接线；切换 inspector 页签保持 PTY，会在折叠右侧面板时卸载并关闭。
- [x] 将 Markdown 资料导入弹窗迁入 `features/workspace`，由应用入口只负责打开状态和导入结果协调。
- [ ] 将设置弹窗、资料导入弹窗、顶部工具栏和菜单从 `App.tsx` 抽到所属 feature/app 模块。
- [ ] 将左右面板尺寸、拖拽和全局快捷键分别收敛到应用级 hooks。
- [ ] 提取编辑工作区、文档目录、工作区文件列表、查找替换栏和视图切换组件。
- [ ] 将章节选择、编辑范围、预览模式、查找替换、折叠章节和 Agent 选区快照收敛到编辑会话控制器。
- [ ] `App.tsx` 最终只保留项目会话装配、顶层布局和跨功能协调，目标控制在 500 行左右。

### P7.3 服务、导出与 Rust 边界

- [ ] 配合 P1 将 Agent 会话存储拆为统一接口、Browser adapter 和 SQLite adapter，React 组件不得直接访问持久化细节。
- [ ] 配合 P2 将工作区监听、磁盘基线和冲突处理收敛到 workspace service/controller。
- [ ] 将 DOCX 导出拆为 Markdown 解析、样式、图片解析、文档构建和保存适配模块，并补齐表格、图片和异常路径测试。
- [ ] 将状态初始化和领域装配下沉到对应 Rust 模块；`lib.rs` 只保留插件、state 和显式命令注册。

## P6 Agent Skills 与技能市场

目标：以 Agent Skills 渐进披露协议扩展 Agent 能力，所有实际执行继续通过应用受控工具与会话权限完成。

- [x] 支持 `SKILL.md` frontmatter，并兼容 `skill.json` 与 README 回退格式。
- [x] 支持内置、全局 app-data 与工作区 `.gouan/skills/` 三层发现；普通 Skill 工作区同名项优先，内置管理 Skill 名称受保护。
- [x] 增加路径越界、符号链接、文件大小、脚本 shebang、ZIP 解包和原子安装校验。
- [x] 增加创建、安装、校验、删除、打包、ClawHub 搜索/详情/安装/更新检查接口。
- [x] 会话 SQLite 保存 `enabledSkills`；Agent 只注入摘要并通过 `skills_manager` 按需读取全文。
- [x] 增加受控 `skill_run_command`，限制程序、工作目录、环境、超时和输出，并逐次确认。
- [x] 内置 DOCX、Excel、agent-browser、skills-creator、skills-installer 五个 Skill；两个管理 Skill 始终启用，并提供 Python/Node/CLI 运行环境诊断。
- [x] 设置页增加已安装、ClawHub、本地安装、创建和运行环境视图。
- [ ] 在 Tauri 桌面端手工验证 ClawHub 实际安装与更新、DOCX/XLSX 产物以及 agent-browser 导航和截图。
- [ ] 为市场下载补充发布者签名或可验证哈希；当前仅执行传输状态与本地结构校验。

## P0 当前改动收尾

当前工作区已有章节删除、章节移动、自定义模板和 Agent 会话持久化相关改动，合并前需要统一收尾：

- [ ] 核对章节删除行为：禁止误删文档标题，删除父章节时包含全部子章节，删除后章节选择仍有效。
- [ ] 核对章节移动行为：不能移动到自己的子树中，移动后标题编号和目录同步更新。
- [ ] 为自定义模板补齐保存、读取、应用、删除和异常数据测试。
- [ ] 验证浏览器与桌面端的模板存储边界：浏览器使用 localStorage，桌面使用 `<workspace.root>/.gouan/templates/`。
- [ ] 完成 Agent 会话从 legacy localStorage / JSON 迁移到工作区 SQLite 后的兼容、并发刷新和错误处理测试。
- [x] 增加忽略规则并清理不应提交的运行数据，包括工具日志/快照、临时脚本、本机 Agent 状态、`workspace/.gouan/*.db` 和 Agent 会话实例文件。
- [x] 2026-07-29 完成前端全量测试（33 个文件、177 项测试）和生产构建。
- [x] 2026-07-29 完成 Agent 会话 Rust 定向测试（2 项通过）。

## P1 Agent 会话存储 SQLite 改造

目标：桌面端以 `<workspace.root>/.gouan/conversations.db` 作为 Agent 会话唯一真源；前端以内存运行态为即时数据源，通过增量事件同步持久化结果。发送、切换、新建、删除和修改会话开关均不得依赖整页刷新或整库重载。

### 存储模型

- [x] 新建独立 Rust `agent_conversations` 存储模块，不复用应用数据目录 `workspace.db`，也不混入记忆模块 `memory.db`。
- [x] 创建 `agent_conversation` 表：`id`、`project_id`、`title`、`summary`、三个会话开关、`created_at`、`updated_at`、`revision`。
- [x] 创建 `agent_conversation_message` 表：`conversation_id`、`sequence`、`role`、`message_json`、`created_at`，以 `(conversation_id, sequence)` 为主键并启用级联删除。
- [x] 创建 `agent_conversation_meta` 表记录 schema 版本和 JSON 迁移完成标记。
- [x] 为 `project_id + updated_at`、`conversation_id + sequence` 建立索引。
- [x] 数据库连接启用 `foreign_keys`、`busy_timeout` 和 WAL，并写入 `PRAGMA user_version`。
- [ ] 将 schema 初始化和版本升级放入事务，按 `user_version` 执行可回滚的增量迁移。
- [x] `message_json` 保留完整 `AgentMessage` 契约，避免拆散 tool call、tool result 和扩展字段；列表查询不得读取消息 JSON。

### Tauri 命令与事件

- [x] 增加 `agent_conversation_list(project_id, workspace_root)`，只返回会话摘要和开关。
- [x] 增加 `agent_conversation_get(id, workspace_root)`，按 `sequence` 返回单个完整会话。
- [x] 增加 `agent_conversation_upsert(input, workspace_root)`，在一个事务中更新元数据和消息；使用 `revision` 检测陈旧写入。
- [x] 增加 `agent_conversation_patch(id, patch, expected_revision)`，用于标题和会话开关的局部更新，不重写消息。
- [x] 增加 `agent_conversation_delete`、`agent_conversation_clear_project`，返回受影响 ID。
- [x] 所有成功变更发送统一 `agent-conversations:changed` 事件，前端增量处理 `saved`、`deleted`、`cleared` 载荷。
- [ ] 所有数据库操作放入阻塞线程；同一会话写入在前端继续串行排队，Rust 事务作为最终一致性边界。

### JSON 迁移与兼容

- [x] 首次打开工作区数据库时，如果未标记迁移且存在 `.gouan/agent-conversations.json`，解析、校验并在单个事务中导入。
- [ ] 按会话 ID 幂等导入；非法会话跳过并返回可见警告，不能导致整个数据库不可用。
- [x] 导入成功后写入迁移标记；旧 JSON 保留为只读备份，不再由新代码写入。
- [ ] localStorage 继续作为浏览器模式适配器；桌面端只在 SQLite 尚无数据且 JSON 迁移源不存在时执行一次 legacy localStorage 导入。
- [ ] 迁移失败时继续使用旧存储读取且禁止覆盖 JSON；修复后可安全重试。

### 前端运行态

- [ ] 将 `conversationStore.ts` 拆为统一接口和 Browser/SQLite 两个 adapter，React 组件不得直接调用 `invoke` 或读取 JSON。
- [ ] 建立按 `conversationId` 索引的运行时缓存，保存消息、事件、Todo、草稿审核、运行状态和 AbortController。
- [x] 切换会话优先同步激活运行时缓存；缓存未命中时只加载目标会话，不重新加载会话列表。
- [ ] 新建会话先乐观插入空运行态，再后台持久化；失败只回滚该会话。
- [x] 删除会话先局部移除，成功事件确认；删除当前会话时只激活内存中的下一条，不重新查询列表。
- [x] 会话开关先更新运行态并调用 `patch`；不得触发完整会话 upsert 或时间线重建。
- [ ] Agent 运行中的 token、工具事件和 Todo 只更新运行态；在稳定检查点或结束时持久化，不因切换面板而 abort。
- [x] Agent 自动压缩检查点升级为 token 原生结构化台账，合并旧检查点而非递归嵌套；按预算动态减少近期消息并压缩较早工具结果，仍超限时在请求模型前显式失败。
- [ ] 订阅 Rust 增量事件并按 `revision` reconcile；忽略旧事件，禁止以整列表替换当前运行态。
- [ ] 限制闲置完整会话缓存数量并使用 LRU 淘汰；当前会话、运行中会话和待审核草稿不得淘汰。

### 测试与切换

- [x] Rust 已覆盖级联删除和基础 revision 冲突。
- [ ] Rust 补齐 schema 初始化、版本迁移、事务回滚、分页列表和单会话读取测试。
- [ ] 覆盖 JSON 正常迁移、重复迁移、部分非法数据、空文件、损坏文件和 localStorage legacy 导入。
- [ ] 覆盖重叠保存严格串行、后写不被先写覆盖、失败写不推进 revision、数据库 busy 后可重试。
- [ ] 前端覆盖发送中切换会话、后台完成后回切、新建、删除、三个开关、外部增量事件和 LRU 淘汰。
- [ ] 增加回归断言：上述操作不得调用完整列表查询，不得卸载 Agent runner，不得触发页面 reload。
- [x] 桌面端直接使用 SQLite，浏览器模式保留 localStorage；旧 JSON 仅作为只读迁移源。
- [ ] 完成剩余 Rust 测试和 Tauri 手工流程；确认旧 JSON 始终只读且迁移失败可安全重试。

## P2 工作区实时同步

目标：工作区文件被终端、Agent 或其他编辑器修改后，应用局部同步相关状态，不再依赖整页刷新或手动“重新加载”。

- [ ] 在 Tauri 后端监听工作区 Markdown、`knowledge/` 和 `.gouan/` 的创建、修改、删除及重命名事件。
- [ ] 对连续文件事件去抖并合并，将标准化的变更路径和事件类型发送给前端。
- [ ] 打开或保存正文时记录磁盘内容哈希、修改时间和已保存正文，区分磁盘版本与本地未保存修改。
- [ ] 当前文件在本地无修改时自动从磁盘重载，只更新正文、目录和文件列表，不刷新整个页面。
- [ ] 当前文件存在本地修改时禁止自动覆盖，展示冲突状态并提供查看差异、使用磁盘版本和保留当前版本操作。
- [ ] 正确处理当前文件被外部删除或重命名后的编辑器状态和文件列表。
- [ ] 保存后更新磁盘基线，并通过内容哈希忽略应用自身写入产生的监听事件。
- [ ] 窗口重新获得焦点时执行一次轻量元数据扫描，补偿遗漏事件；浏览器模式使用该机制作为降级方案。
- [ ] 保留手动“重新加载”作为异常兜底，并覆盖自动同步、冲突、自写入去重、删除和重命名测试。

## P3 Agent 文档编辑工具

目标：让 Agent 根据用户指令和当前编辑上下文，自动选择章节改写、选区修改、章节插入或章节删除工具；所有操作都必须经过审核。

### 知识检索可靠性

- [x] 将自然语言查询压缩为有限核心词，过滤常见疑问词、动作词和连接词，并限制检索词数量以避免 FTS 表达式失控。
- [x] 使用“全部核心词匹配 -> 少一个词匹配 -> OR 宽松匹配”的零召回级联；每层去重，命中后停止继续放宽，避免精确结果被噪声淹没。
- [x] 改为相关性优先排序，质量等级只提供小幅加权；覆盖标题、正文和质量冲突的回归测试。
- [x] `search_knowledge` 直接向 Agent 返回受预算限制的正文证据、章节 ID、相关性、质量和截断状态；仅在截断或证据不足时调用 `read_knowledge`。
- [x] 为未配置工作区、空召回、数据库异常和无效章节 ID 增加模型可见的稳定错误码与重试提示。
- [ ] 将“一章节一片段”升级为固定长度、带重叠且保留章节路径的切片策略，并通过版本升级安全重建已有索引。
- [ ] 增加本地向量召回，与 FTS 关键词召回、标题路径权重和质量标记组成混合检索。
- [ ] 建立真实技术方案查询评测集，持续统计 Recall@K、首条命中率、无结果率和 Agent 二次改写成功率。

### 统一编辑提案

- [x] 将现有 `AgentDraft` 扩展为带操作类型和目标信息的统一编辑提案。
- [x] 支持 `replace_section`、`replace_selection`、`insert_section`、`delete_section` 四种操作。
- [x] 提案记录目标章节、原文快照、选区范围、修改说明和必要的基准校验信息。
- [x] 接受提案前校验目标和原文快照；文档已变化时报告冲突，不覆盖新内容。

### Markdown 操作

- [x] 在 `src/features/editor/markdownDoc.ts` 提供插入章节、删除章节、替换选区和统一应用提案的纯函数。
- [x] 插入章节支持指定章节的 `before` / `after` 位置，并保持 Markdown 间隔正确。
- [x] 删除章节包含其正文和全部子章节，禁止通过 Agent 删除文档 H1。
- [x] 插入或删除后按现有规则重新编号标题。
- [x] 选区替换同时支持章节编辑和全文编辑模式；编辑器失焦前捕获选区快照，在 Agent 面板显示状态，并将选区正文注入本轮临时模型上下文。

### Agent 工具注册

- [x] 已有 `read_current_section` 和 `propose_section_update`，支持改写当前章节并等待用户审核。
- [x] 增加 `read_selected_text` 和 `read_proposal_section`。
- [x] 增加 `propose_selection_update`、`propose_section_insert`、`propose_section_delete`。
- [x] 增加 `propose_section_move`，支持将章节及其子章节移动到目标章节前后，禁止移动 H1、自身或移入自身子树。
- [x] 没有非空选区时不注册选区修改工具；没有有效章节时不暴露对应章节工具。
- [x] 系统提示词明确工具选择规则，工具只提交提案，不得声称已经写入文件。
- [x] 增加 `review_content` 内容审核工具：以隔离模型调用按 1～20 条明确要求检查全文、当前章节、指定章节或选区，原文证据锚定后返回通过、未通过和待确认项，不自动修改正文。

### 审核与应用

- [x] 审核界面根据操作类型显示章节前后对照、选区前后对照、插入位置或删除警告。
- [x] 章节移动审核同时展示源章节和目标位置，并在接受时校验双方快照、移动后自动重新编号。
- [x] 接受一项提案只产生一条撤销记录。
- [x] 应用后恢复合理的当前章节、光标和滚动位置。
- [ ] 补齐审核取消和运行中目标变化的 UI 集成测试；现有单元测试已覆盖接受、拒绝、目标丢失、原文冲突和连续提案基线。

## P4 AI 按章节通篇编写/修改长任务模式

目标：以确定性的 Coordinator + 限并发章节 Worker Pool 完成长文档编写和修改；Worker 仅执行结构化模型调用，不拥有文件或普通 Agent 工具。

### 领域模型与章节解析

- [x] 独立定义 `LongWritingMode`、任务/章节状态、`OutlinePlan`、`ChapterJob` 和 `ConsistencyIssue`，不混入 `Project` 或普通 Agent 会话。
- [x] 以 H2 及其完整子树为工作单元，H1 作为文档标题；稳定解析忽略代码围栏内井号，支持重复标题、中文标题、空章节和无 H2 文档。
- [x] 为任务记录基础/当前全文 hash，为章节记录原文/提交目标 hash；API Key 不进入任务记录。
- [ ] 补充超大文档的分段摘要预算基准和压力测试。

### 规划、目录审核与 Worker Pool

- [x] 支持补写空白/短章节、通篇改写所选章节、按指令修改受影响章节三种模式。
- [x] 规划阶段生成 Document Bible：摘要、受众、规则、固定事实、术语、章节目标、衔接要求和最终处理范围。
- [x] 长文档先以隔离摘要 Worker 限并发提取章节摘要、固定事实、术语和待确认项，再交给规划 Coordinator 汇总。
- [x] 长任务上下文按模型窗口感知预算：为摘要、规划、章节生成和一致性检查预留输出 token，输入预算取用户阈值与可用窗口的较小值；附加资料按章节目标筛选为相关片段再截断/省略，遇到服务端上下文超限时自动缩减预算重试；规划和一致性检查可生成保留 H2 骨架的全文概览，当前章节自身仍超限则显式失败。
- [x] 目录差异必须先由用户审核；确认后一次性应用并冻结全部标题层级和顺序。
- [x] Worker 使用强制 `submit_chapter_draft` 结构化输出，只接收冻结规则、当前章节、相邻 brief、明确附加资料和已提交摘要。
- [x] 默认并发 2、最大 3；避免同时启动相邻章节，完成后进入唯一串行提交队列。
- [x] 支持暂停、继续、停止、失败重试和请求取消；429/超时采用退避重试。

### 桌面持久化与文件安全

- [x] 首版仅桌面端和已保存的 workspace Markdown 开放；启动前先保存并创建不可变时间戳备份。
- [x] SQLite 持久化任务和章节状态；重启时将中断的 worker 重置排队并核对提交中的章节。
- [x] `commit_long_task_chapter` 以全文 hash + 章节 hash 做 compare-and-swap，串行原子替换文件，外部修改进入冲突状态。
- [x] 停止只取消未完成请求并保留已提交章节；恢复原文前先创建 `pre-restore` 快照，再恢复最初备份。
- [x] 任务运行期间锁定正文编辑、打开其他文件、重命名和删除当前文件；关闭应用后可继续、终止或恢复。
- [ ] 增加备份管理和清理策略；当前版本保留全部任务备份。

### UI、一致性检查与验收

- [x] Agent 区提供独立“长任务”入口和启动向导：模式、总指令、章节范围、资料、模型及并发数。
- [x] 增加“从零创建完整方案”模式：仅允许空白或 H1-only 的已保存文档，用户填写标题并勾选资料后生成可审核 H2 目录；目录确认后再按章写作。
- [x] 目录阶段展示冻结结构、章节目标和最终处理范围；确认后才启动正文生成。
- [x] 运行界面展示总进度和每章排队、运行、校验、写入、完成、失败状态，并提供暂停、继续、停止、单章重试和恢复原文。
- [x] 运行界面提供可恢复的执行动态、章节展开预览、原文/AI 稿差异、摘要/事实/术语/待确认项，以及编辑器章节定位。
- [x] 完成后执行本地结构检查和模型一致性检查；问题不自动修改，仅对用户勾选确认的问题创建修正任务，并在修正前快照。
- [ ] 使用至少 9 个 H2 章节的真实方案完成三种模式、乱序并发提交、关闭重启继续、停止恢复和 DOCX 导出手工验收。
- [ ] 补齐 Tauri 集成级崩溃注入测试（提交标记后、原子替换前后）。

## 长任务模式暂不包含

- 模型递归创建自治 subagent，或 Worker 直接操作文件。
- Browser/Tauri 等价运行；首版仅桌面端。
- 多模型竞争同一章节，或正文阶段改变已冻结目录。
- 自动清理历史备份。

## P5 桌面与交付验证

- [x] 配置 GitHub Actions：主分支 / PR 构建 NSIS EXE Artifact，版本 tag 构建并发布带签名更新清单的 NSIS/MSI Release。
- [ ] 在安装 Rust stable、MSVC 和 WebView2 的 Windows 环境运行 `pnpm tauri dev`。
- [ ] 验证系统凭据、工作区 SQLite、文件打开/保存、图片资源、PTY 终端和受控命令。
- [ ] 运行 `pnpm tauri build`，验证 NSIS/MSI 安装产物和升级路径。
- [ ] 使用实际 Word 阅读器检查 DOCX 的标题、目录、表格、本地图片、分页和中文字体效果。
