# 全文优化功能待办

> 目标：在现有“单块 AI 改写 + diff 接受/拒绝”能力上，增加“生成计划 -> 逐章优化 -> 分章审核/全文接受 -> 一致性复核”的全文优化流程。

## MVP 范围

- [ ] AI 面板增加作用范围切换：`当前章节` / `全文`。
- [ ] 全文优化由应用编排，不依赖模型 Tool Calling。
- [ ] 开始前生成结构化全文计划，并允许用户确认后执行。
- [ ] 按 Markdown 章节逐个生成草稿，显示等待、生成中、完成、失败和冲突状态。
- [ ] 支持单章接受/拒绝、接受全部/拒绝全部。
- [ ] 接受全部时原子更新 `project.markdown`，但不直接覆盖工作区文件；磁盘写入继续使用现有“保存”。
- [ ] 生成期间用户修改原文时检测冲突，禁止旧草稿静默覆盖新内容。

## 1. 领域模型

- [ ] 在 `src/types.ts` 定义全文优化类型：
  - `DocumentOptimizationPlan`：全文目标、风格规则、术语、章节计划。
  - `DocumentSectionContext`：章节 ID、标题、范围、摘要、依赖。
  - `DocumentOptimizationDraft`：原文、优化后正文、状态、错误和基准哈希。
  - `DocumentOptimizationSession`：任务状态、当前进度、计划、全部章节草稿、创建时间。
- [ ] 为任务保存 `baseMarkdownHash`，为每章保存 `beforeHash`。
- [ ] 明确会话是否需要持久化。MVP 建议只保存在 React 状态中；关闭或切换文件前提示未处理草稿。
- [ ] 不把模型 API Key 或完整资料内容加入新增持久化对象。

## 2. Markdown 章节解析

- [ ] 复用 `src/markdownDoc.ts` 的标题语义，新增稳定的章节切分函数。
- [ ] 章节对象记录标题行、正文起止位置和原始文本，避免用不可靠的全局字符串替换。
- [ ] 定义范围：MVP 按 H1 章节处理；没有 H1 时按最高标题层级处理；完全没有标题时视为一个章节。
- [ ] 保留标题层级、代码块、表格、图片路径和链接。
- [ ] 添加解析测试：重复标题、空章节、中文标题、围栏代码中的 `#`、无标题文档。

## 3. 上下文与计划

- [ ] 新增上下文构建模块，例如 `src/documentOptimization/context.ts`。
- [ ] 规划阶段生成：全文摘要、受众、写作规则、术语表、关键事实和各章目标/依赖。
- [ ] 短文档可用全文生成计划；长文档先逐章摘要，再汇总为计划。
- [ ] 定义可配置的上下文预算，按优先级组装：
  1. 系统规则和用户要求
  2. 全文摘要、目录、术语和关键事实
  3. 当前章节完整原文
  4. 相邻章节摘要或边界片段
  5. 依赖章节片段
  6. 用户明确附加的资料片段
- [ ] 超出预算时优先裁剪低优先级资料，不裁剪当前章节原文；当前章节本身过长时提示用户或分块处理。
- [ ] 只发送 `contextSourceRefs` 明确附加的资料，并在 UI 中显示本次将使用的资料。
- [ ] 每章完成后更新滚动摘要、术语和衔接信息，供后续章节使用。
- [ ] 要求模型不得虚构事实，只返回当前章节 Markdown，不返回代码围栏或解释。

## 4. 模型服务

- [ ] 在 `src/services/model.ts` 增加独立接口：生成全文计划、生成章节草稿、执行一致性检查。
- [ ] Browser/Tauri 两条运行路径保持相同的请求和返回契约。
- [ ] 为结构化计划增加严格解析和错误提示；不要依赖自由文本提取字段。
- [ ] 支持取消当前任务，并确保取消后不会继续写入草稿状态。
- [ ] 单章失败支持重试，重试不能丢失其他已完成章节。
- [ ] 增加模型服务测试：正常响应、流式响应、非法计划、网络错误、取消和重试。

## 5. AI 面板交互

- [ ] 在 AI 面板增加作用范围分段控件：`当前章节` / `全文`。
- [ ] 全文模式提供优化目标：语言润色、专业表达、内容精简、结构优化、自定义要求。
- [ ] “结构优化”默认关闭；关闭时禁止模型改变标题和章节顺序。
- [ ] 先展示优化计划，用户确认后才开始逐章调用模型。
- [ ] 显示总进度、当前章节、各章状态、失败原因和停止/重试操作。
- [ ] 每章可打开 before/after diff，并单独接受或拒绝。
- [ ] 提供“接受全部”和“拒绝全部”；明确显示可接受、失败及冲突章节数量。
- [ ] 任务执行时允许用户继续查看文档，但编辑正文后应立即重新计算冲突状态。
- [ ] 切换文件、关闭右侧面板或启动新任务时，提示仍有未处理草稿。

## 6. 应用草稿与全文接受

- [ ] 单章接受必须根据记录的章节范围和 `beforeHash` 应用，不能仅按标题查找。
- [ ] 全文接受前重新解析当前 Markdown，并校验 `baseMarkdownHash`/各章 `beforeHash`。
- [ ] 全文接受先在内存中合成完整 Markdown，所有校验通过后一次性调用 `setProject`。
- [ ] 对用户在生成期间修改过的章节标记为 `conflict`，不允许静默覆盖。
- [ ] 失败或冲突章节默认不进入“接受全部”；确认对话框必须列出遗漏章节。
- [ ] 接受前保存完整 Markdown 快照，提供一次“撤销全文接受”。
- [ ] 接受草稿不会调用 `write_text_file`；继续由工具栏“保存”决定何时写盘。

## 7. 最终复核

- [ ] 本地结构校验：标题层级/编号、围栏代码、表格、图片和链接是否被破坏。
- [ ] 模型一致性检查只先发送目录、章节摘要、术语和关键事实。
- [ ] 检查发现疑点后，仅补充相关章节片段，不重新发送全文。
- [ ] 将问题作为可审核建议展示，不自动二次改写已经接受的正文。

## 8. 测试与验收

- [ ] 单元测试覆盖章节解析、上下文预算、哈希冲突、草稿合成和原子接受。
- [ ] 组件测试覆盖计划确认、进度状态、单章接受/拒绝、接受全部和失败重试。
- [ ] 回归验证现有单块 `AiDraft` 流程不受影响。
- [ ] 验证 API Key 仍不会写入 `tech-proposal-studio.project.v1`。
- [ ] 验证未显式附加的资料不会进入请求上下文。
- [ ] 浏览器模式和 Tauri 模式至少各完成一次手工流程。
- [ ] 运行 `pnpm test` 和 `pnpm build`。

## 建议实施顺序

1. 章节解析、哈希与领域类型。
2. 计划及章节优化模型接口。
3. 上下文预算和滚动摘要。
4. 全文任务状态机及 AI 面板进度 UI。
5. 分章审核、全文接受、冲突检测和撤销。
6. 一致性复核、测试和双运行时验收。

## MVP 暂不包含

- 模型自主调用 `read_section` / `replace_section` 等工具。
- AI 自动写入工作区 Markdown 文件。
- 跨设备或跨会话恢复生成任务。
- 自动调整章节顺序、删除章节或新建章节。
- 多模型并行优化同一文档。

## 完成标准

使用一份至少 9 章的真实方案，用户能够确认全文计划、观察逐章生成、审核任意章节，并通过一次“接受全部”安全更新编辑器内容；期间手工修改任一章节时，该章节必须报告冲突且不会被旧草稿覆盖。最终测试和构建通过，工作区文件只有在用户点击“保存”后才发生变化。
