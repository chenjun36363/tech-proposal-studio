# 2026-07-31

- 修复 build 后 exe 启动附带控制台窗口的问题：`src-tauri/src/main.rs` 缺少 `#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`，已补回（Tauri 模板标准写法，release 下走 GUI 子系统，debug 下保留控制台便于看日志）。cargo check 通过。需重新 `pnpm tauri build` 生效。
