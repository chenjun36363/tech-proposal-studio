# 2026-07-29

## 右侧 Agent/AI 面板：模型选择支持模糊搜索
- 把 `src/components/ModelSelect.tsx` 从原生 `<select>` 升级为可搜索 combobox。
- 搜索域覆盖 `catalog ∪ activeModels`（按供应商，复用 `ProviderEditModal` 思路）。
- 新增 `src/utils/fuzzy.ts`：零依赖子序列模糊打分 `fuzzyScore` / `fuzzyFilter`；子串优先、连续命中加分、短文本优先。
- 支持键盘 ↑/↓、Enter、Esc，点击外部 / 滚动关闭；搜索框按 模型/displayName/ownedBy/供应商/协议 匹配。
- `src/services/llm/resolve.ts`：requireActive 校验现在也认可 `catalog` 中的模型（增量修改），避免从 combobox 选中 catalog 模型时报"未启用"。
- `src/index.css`：新增 `.model-combobox*` 样式（触发按钮 + 浮层 + 列表 + 选项）。
- 验证：`tsc -b` 通过；`vitest run` 全绿（158 用例，含 protocols.test 24 项）；`pnpm build` 成功。
- 注意：右侧 `.inspector-content` 是 `overflow:auto`，combobox 在面板顶部，浮层常规不会被裁；已加滚动关闭防止错位。
